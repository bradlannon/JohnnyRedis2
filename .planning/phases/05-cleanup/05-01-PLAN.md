---
phase: 05-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/payloads.ts
  - packages/shared/src/topics.ts
  - packages/shared/src/payloads.test.ts
  - packages/shared/src/topics.test.ts
  - packages/web/src/lib/sendCommand.ts
  - packages/web/src/components/SchedulerUI.tsx
autonomous: true
requirements: [TECH-DEBT]

must_haves:
  truths:
    - "HeartbeatPayload and TOPICS.heartbeat are completely removed from source and tests"
    - "sendCommand value parameter is optional, matching CommandPayload schema"
    - "'tone' action appears in SchedulerUI action picker"
    - "RETAIN export has TSDoc comment explaining hub-only usage"
    - "All existing tests pass (95 expected after heartbeat removal)"
    - "TypeScript compiles cleanly across all packages"
  artifacts:
    - path: "packages/shared/src/payloads.ts"
      provides: "HeartbeatPayload removed"
      contains: "CommandPayload"
    - path: "packages/shared/src/topics.ts"
      provides: "heartbeat removed from TOPICS and RETAIN, TSDoc on RETAIN"
      contains: "Used by the hub only"
    - path: "packages/web/src/lib/sendCommand.ts"
      provides: "Optional value parameter"
      contains: "value?"
    - path: "packages/web/src/components/SchedulerUI.tsx"
      provides: "tone action in picker"
      contains: "'tone'"
  key_links:
    - from: "packages/web/src/lib/sendCommand.ts"
      to: "packages/shared/src/payloads.ts"
      via: "CommandPayload.value optionality alignment"
      pattern: "value\\?"
---

<objective>
Clean up code-level tech debt identified in the v1.0 milestone audit: remove dead heartbeat code, align sendCommand types with CommandPayload schema, add missing 'tone' action to SchedulerUI, and document RETAIN export intent.

Purpose: Eliminate orphaned code, fix type misalignment, and close a behavioral gap in the scheduler UI.
Output: Clean shared/web packages with all tests passing and TypeScript compiling cleanly.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cleanup/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead heartbeat code from shared package</name>
  <files>packages/shared/src/payloads.ts, packages/shared/src/topics.ts, packages/shared/src/payloads.test.ts, packages/shared/src/topics.test.ts</files>
  <action>
Remove all heartbeat-related code from the shared package. This is dead code with zero consumers outside the shared package itself.

In payloads.ts:
- Remove the HeartbeatPayload Zod schema and its type export (lines 22-30 area). Keep all other schemas intact.

In topics.ts:
- Remove `heartbeat: () => 'home/hub/heartbeat'` from the TOPICS object (line 5 area)
- Remove `heartbeat: false` from the RETAIN object (line 13 area)
- Add a TSDoc comment above the RETAIN export explaining hub-only usage:
  ```typescript
  /**
   * Retain flags for MQTT publishes.
   * Used by the hub only - the server is a subscribe-only MQTT client.
   */
  ```

In payloads.test.ts:
- Remove the HeartbeatPayload import from line 2
- Remove the entire HeartbeatPayload describe block (lines 56-65 area)

In topics.test.ts:
- Remove the heartbeat topic test (lines 17-19 area): `expect(TOPICS.heartbeat()).toBe(...)`
- Remove the RETAIN.heartbeat test (lines 34-36 area): `expect(RETAIN.heartbeat).toBe(false)`

After removal, run `npx vitest run` to confirm test count drops by 4 (from 99 to ~95) with all remaining tests passing, then run `npx tsc --build` to confirm clean compilation.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/JohnnyRedis && npx vitest run && npx tsc --build</automated>
  </verify>
  <done>HeartbeatPayload schema, type, TOPICS.heartbeat, and RETAIN.heartbeat are fully removed from source and tests. RETAIN has a TSDoc comment. All remaining tests pass. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Fix sendCommand optional value and add tone to SchedulerUI</name>
  <files>packages/web/src/lib/sendCommand.ts, packages/web/src/components/SchedulerUI.tsx</files>
  <action>
Two small fixes in the web package to align types and close a UI gap.

In sendCommand.ts:
- Make the `value` parameter optional: `value?: string | number | boolean`
- Update the JSON body to conditionally include value: `body: JSON.stringify({ device, board, action, ...(value !== undefined ? { value } : {}) })`
- This aligns with CommandPayload.value which was made optional in Phase 4. All existing callers still pass value so no caller changes needed.

In SchedulerUI.tsx:
- Update the Action type union to include 'tone': `type Action = 'on' | 'off' | 'set' | 'tone'`
- Update the ACTIONS array to include 'tone': `const ACTIONS: Action[] = ['on', 'off', 'set', 'tone']`
- This allows scheduling piezo tone commands, which use action='tone' (as seen in PiezoControl.tsx).

Run `npx tsc --build` to verify type alignment, then `npx vitest run` to confirm no regressions.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/JohnnyRedis && npx tsc --build && npx vitest run</automated>
  </verify>
  <done>sendCommand accepts optional value matching CommandPayload schema. SchedulerUI action picker includes 'tone'. TypeScript compiles cleanly and all tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx vitest run` — all tests pass (expect ~95 after heartbeat removal)
2. `npx tsc --build` — zero TypeScript errors across all packages
3. `grep -r "HeartbeatPayload" packages/` — zero results
4. `grep -r "TOPICS.heartbeat" packages/` — zero results (except possibly this plan file)
</verification>

<success_criteria>
- Dead heartbeat code fully removed (source + tests)
- sendCommand value parameter is optional
- SchedulerUI includes 'tone' in action picker
- RETAIN has TSDoc comment
- All tests pass, TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-cleanup/05-01-SUMMARY.md`
</output>
