---
phase: 05-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
autonomous: false
requirements: [TECH-DEBT]

must_haves:
  truths:
    - "Camera environment variables are documented in .env.example"
    - "Drizzle migration has been applied to production Neon database (or confirmed already applied)"
  artifacts:
    - path: ".env.example"
      provides: "Complete env var documentation including camera URLs"
      contains: "VITE_CAMERA_WEBCAM_URL"
  key_links: []
---

<objective>
Complete project documentation and apply the pending database migration: add camera env vars to .env.example and run the Drizzle migration against the production Neon database.

Purpose: Ensure deployment documentation is complete and production database has the required tables.
Output: Updated .env.example with camera vars; production Neon database with sensor_readings and scheduled_actions tables.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cleanup/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add camera env vars to .env.example</name>
  <files>.env.example</files>
  <action>
Append the two missing camera environment variables to `.env.example` with placeholder values and a comment explaining they are optional (Vite build-time only, web package).

Add after the existing DATABASE_POOLER_URL line:

```
# Camera HLS stream URLs (optional, web package only - Vite build-time substitution)
# If omitted, CameraSection tabs won't render (graceful degradation)
VITE_CAMERA_WEBCAM_URL=http://your-mediamtx-host:8888/webcam/index.m3u8
VITE_CAMERA_PI_URL=http://your-mediamtx-host:8888/picamera/index.m3u8
```
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/JohnnyRedis && grep "VITE_CAMERA_WEBCAM_URL" .env.example && grep "VITE_CAMERA_PI_URL" .env.example</automated>
  </verify>
  <done>.env.example contains VITE_CAMERA_WEBCAM_URL and VITE_CAMERA_PI_URL with placeholder values and explanatory comments.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Apply Drizzle migration to production Neon database</name>
  <action>
The Drizzle migration at `packages/server/drizzle/0000_workable_raza.sql` creates the `sensor_readings` and `scheduled_actions` tables. This must be run against the production Neon database using the DIRECT connection URL (not the pooler URL).

**IMPORTANT:** Use `DATABASE_URL` (direct Neon connection), NOT `DATABASE_POOLER_URL` (PgBouncer pooler). The pooler does not support DDL operations needed for migrations.
  </action>
  <how-to-verify>
Run the migration from the packages/server directory:

```bash
cd packages/server
DATABASE_URL=<your-direct-neon-url> npx drizzle-kit migrate
```

Then verify tables exist:

```bash
psql <your-direct-neon-url> -c "\dt"
```

Expected output should include `sensor_readings` and `scheduled_actions` tables.

If the tables already exist (migration was previously applied), drizzle-kit migrate will report nothing to do — this is safe and expected.
  </how-to-verify>
  <resume-signal>Type "migration applied" once tables are confirmed, or "skip" if tables already exist, or describe any errors.</resume-signal>
</task>

</tasks>

<verification>
1. `.env.example` contains all 7 env vars (5 existing + 2 camera)
2. Production Neon database has sensor_readings and scheduled_actions tables
</verification>

<success_criteria>
- .env.example documents all environment variables needed for deployment
- Drizzle migration applied (or confirmed already applied) to production Neon
</success_criteria>

<output>
After completion, create `.planning/phases/05-cleanup/05-02-SUMMARY.md`
</output>
