---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - packages/server/src/sse/index.ts
  - packages/server/src/sse/sse.test.ts
  - packages/server/src/db/client.ts
  - packages/server/src/db/schema.ts
  - packages/server/src/index.ts
  - packages/server/drizzle.config.ts
  - packages/hub/src/mqtt/client.ts
  - packages/hub/src/index.ts
  - vitest.config.ts
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

user_setup:
  - service: hivemq-cloud
    why: "MQTT broker for Pi-to-server communication"
    env_vars:
      - name: HIVEMQ_HOST
        source: "HiveMQ Cloud Console -> Cluster Overview -> hostname (e.g., xxx.s1.eu.hivemq.cloud)"
      - name: HIVEMQ_USER
        source: "HiveMQ Cloud Console -> Access Management -> create credentials"
      - name: HIVEMQ_PASS
        source: "HiveMQ Cloud Console -> Access Management -> create credentials"
    dashboard_config:
      - task: "Create a free HiveMQ Cloud cluster"
        location: "https://console.hivemq.cloud/ -> Create New Cluster"
      - task: "Create MQTT credentials (username + password)"
        location: "HiveMQ Cloud Console -> Access Management tab"
  - service: neon-tech
    why: "PostgreSQL database for sensor data persistence"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console -> Connection Details -> Direct connection string"
      - name: DATABASE_POOLER_URL
        source: "Neon Console -> Connection Details -> Pooled connection string (hostname contains -pooler)"
    dashboard_config:
      - task: "Create a free Neon project"
        location: "https://console.neon.tech/ -> New Project"
      - task: "Copy both connection strings (direct and pooled)"
        location: "Neon Console -> Connection Details -> toggle between Direct and Pooled"

must_haves:
  truths:
    - "SSE endpoint sets correct headers including X-Accel-Buffering: no"
    - "SSE broadcast function sends named events to all connected clients"
    - "Hub MQTT client connects to HiveMQ Cloud with LWT and reconnect configured"
    - "Drizzle schema defines sensor_readings table with correct columns"
    - "Server Express app wires SSE router at /events"
  artifacts:
    - path: "packages/server/src/sse/index.ts"
      provides: "SSE endpoint with keepalive and broadcast"
      exports: ["broadcast", "default"]
    - path: "packages/server/src/sse/sse.test.ts"
      provides: "Integration tests for SSE headers and broadcast"
    - path: "packages/server/src/db/schema.ts"
      provides: "Drizzle schema for sensor_readings"
      exports: ["sensorReadings"]
    - path: "packages/server/src/db/client.ts"
      provides: "Neon HTTP connection via Drizzle"
      exports: ["db"]
    - path: "packages/hub/src/mqtt/client.ts"
      provides: "MQTT client with LWT, reconnect, and status publish"
      exports: ["client"]
  key_links:
    - from: "packages/hub/src/mqtt/client.ts"
      to: "@johnnyredis/shared"
      via: "imports TOPICS and RETAIN for topic strings and retain policy"
      pattern: "import.*TOPICS.*RETAIN.*@johnnyredis/shared"
    - from: "packages/server/src/index.ts"
      to: "packages/server/src/sse/index.ts"
      via: "Express app.use mounts SSE router"
      pattern: "app\\.use.*sse"
    - from: "packages/server/src/db/client.ts"
      to: "packages/server/src/db/schema.ts"
      via: "drizzle(sql, { schema })"
      pattern: "drizzle.*schema"
---

<objective>
Wire external services into the monorepo: create the SSE endpoint with integration tests, configure the Drizzle + Neon database schema, and build the hub MQTT client with LWT and reconnect. This plan makes the server ready for SSE push and the hub ready for MQTT publish.

Purpose: Prove the critical infrastructure paths work -- SSE headers correct for Hostinger nginx, MQTT client configured for HiveMQ Cloud with proper LWT, database schema ready for sensor persistence. The SSE spike deployed to Hostinger is the phase gate.
Output: Working SSE endpoint (tested), Drizzle schema (migrated), MQTT client (configured). Ready for Phase 2 feature work.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01-01 shared package (must exist before this plan runs) -->

From packages/shared/src/topics.ts:
```typescript
export const TOPICS = {
  sensor:    (device: string, board: string) => `home/sensor/${device}/${board}`,
  command:   (device: string, board: string) => `home/cmd/${device}/${board}`,
  status:    (board: string)                 => `home/status/${board}`,
  heartbeat: ()                              => `home/hub/heartbeat`,
} as const

export const RETAIN = {
  sensor:    false,
  command:   false,
  status:    true,
  heartbeat: false,
} as const
```

From packages/shared/src/payloads.ts:
```typescript
export const SensorPayload = z.object({ device: z.string(), board: z.string(), value: z.number(), ts: z.number() })
export const StatusPayload = z.object({ online: z.boolean(), ts: z.number() })
export const CommandPayload = z.object({ device: z.string(), board: z.string(), action: z.string(), value: z.union([z.string(), z.number(), z.boolean()]) })
export type SensorPayload = z.infer<typeof SensorPayload>
export type StatusPayload = z.infer<typeof StatusPayload>
export type CommandPayload = z.infer<typeof CommandPayload>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSE endpoint with integration tests and Drizzle + Neon database schema</name>
  <files>
    packages/server/src/sse/index.ts, packages/server/src/sse/sse.test.ts,
    packages/server/src/db/client.ts, packages/server/src/db/schema.ts,
    packages/server/src/index.ts, packages/server/drizzle.config.ts,
    vitest.config.ts
  </files>
  <behavior>
    - GET /events response has Content-Type: text/event-stream
    - GET /events response has X-Accel-Buffering: no
    - GET /events response has Cache-Control: no-cache
    - GET /events response sends retry directive as first message
    - broadcast('sensor:update', { value: 42 }) writes correct SSE format to all connected clients
    - Disconnected clients are cleaned up from the client set
  </behavior>
  <action>
    1. Update vitest.config.ts to add packages/server to the projects array.
    2. Write SSE tests FIRST (red phase) in packages/server/src/sse/sse.test.ts:
       - Test that GET /events sets correct response headers (Content-Type, X-Accel-Buffering, Cache-Control, Connection)
       - Test that broadcast() writes correctly formatted SSE events
       - Use supertest or direct Express app testing (import the router, mount on a test app)
       - Install supertest as devDependency for server package if needed
    3. Create packages/server/src/sse/index.ts following EXACTLY the pattern from RESEARCH.md Pattern 4:
       - Router with GET /events endpoint
       - Set all four required headers BEFORE flushHeaders()
       - Send retry: 10000 directive
       - Register client in Set, cleanup on close
       - 25-second keepalive interval
       - Export broadcast(event, data) function and router as default
    4. Run tests -- they should now pass (green phase).
    5. Create packages/server/src/db/schema.ts: Drizzle pgTable for sensor_readings with id (serial PK), device (text notNull), board (text notNull), value (real notNull), createdAt (timestamp defaultNow notNull). Follow RESEARCH.md Pattern 5 exactly.
    6. Create packages/server/src/db/client.ts: import neon from @neondatabase/serverless, drizzle from drizzle-orm/neon-http, and schema. Export db using DATABASE_POOLER_URL. Guard with a check: if env var missing, log warning but don't crash (allows server to start without DB for SSE-only testing).
    7. Create packages/server/drizzle.config.ts for drizzle-kit: use DATABASE_URL (direct, not pooler) for migrations. Schema path: "./src/db/schema.ts".
    8. Update packages/server/src/index.ts: import and mount the SSE router (app.use(sseRouter)). Keep the /health endpoint. Add CORS headers for development (Access-Control-Allow-Origin: *). Listen on PORT env var or 3000.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/JohnnyRedis && npm install && npx vitest run</automated>
  </verify>
  <done>
    - SSE integration tests pass (correct headers, broadcast format)
    - Drizzle schema file exports sensorReadings table definition
    - DB client exports db instance with Neon pooler connection
    - Server mounts SSE router and responds on GET /events
    - All shared package tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Hub MQTT client with LWT, reconnect, and status publish</name>
  <files>
    packages/hub/src/mqtt/client.ts, packages/hub/src/index.ts
  </files>
  <action>
    1. Create packages/hub/src/mqtt/client.ts following EXACTLY the pattern from RESEARCH.md Pattern 3:
       - Import mqtt from 'mqtt'
       - Import { TOPICS, RETAIN } from '@johnnyredis/shared'
       - Connect to mqtts://${HIVEMQ_HOST}:8883 using HIVEMQ_USER and HIVEMQ_PASS from env
       - Configure reconnectPeriod: 5000, keepalive: 60, clean: true
       - Configure LWT: topic TOPICS.status('hub'), payload { online: false }, qos 1, retain RETAIN.status (true)
       - On 'connect': publish online status to TOPICS.status('hub') with retain: true
       - On 'reconnect': log reconnect attempt
       - On 'error': log error, do NOT crash (let reconnect handle it)
       - Export the client instance
    2. Update packages/hub/src/index.ts:
       - Import dotenv/config at top
       - Import client from ./mqtt/client
       - Log connection state changes
       - Add graceful shutdown: on SIGINT/SIGTERM, publish offline status to TOPICS.status('hub') with retain: true, then client.end()
       - This is a long-running process -- no process.exit() on connect

    NOTE: No automated test for MQTT client -- it requires a live HiveMQ broker. This is verified via manual smoke test (VALIDATION.md 01-02-03). The MQTT client code is straightforward configuration following the researched pattern. Type safety is enforced through @johnnyredis/shared imports.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/JohnnyRedis && npx tsc --build</automated>
  </verify>
  <done>
    - Hub MQTT client compiles without type errors
    - Client configured with LWT (offline status on ungraceful disconnect)
    - Client configured with 5s reconnect period
    - Hub imports TOPICS and RETAIN from @johnnyredis/shared
    - Graceful shutdown publishes offline status before disconnecting
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify SSE spike on Hostinger and MQTT on HiveMQ</name>
  <files>none -- verification only</files>
  <action>
    CHECKPOINT: Human verifies that the two critical infrastructure paths work on their target environments. This is the Phase 1 gate -- SSE must work on Hostinger before Phase 2 begins.

    What was built:
    - SSE endpoint in packages/server with correct headers for nginx proxy
    - MQTT client in packages/hub with LWT and reconnect for HiveMQ Cloud
  </action>
  <verify>
    SSE Verification (CRITICAL -- phase gate):
    1. Deploy server package to Hostinger (or run locally first: cd packages/server && npm start)
    2. Open browser DevTools -> Network tab
    3. Navigate to {server-url}/events
    4. Confirm: response headers include X-Accel-Buffering: no and Content-Type: text/event-stream
    5. Confirm: keepalive comments appear every ~25 seconds (visible in EventStream tab)
    6. If deployed to Hostinger: confirm events are NOT batched/delayed (arrive within 1 second of server write)

    MQTT Verification:
    1. Ensure .env has HIVEMQ_HOST, HIVEMQ_USER, HIVEMQ_PASS populated
    2. Run hub: cd packages/hub && npm start
    3. Confirm: "Connected to HiveMQ" log appears
    4. Confirm: hub publishes online status (check HiveMQ Cloud web client or server subscription)

    Outbound-Only Verification (INFRA-03):
    1. Both MQTT (hub -> HiveMQ) and SSE (server -> browser) use outbound connections
    2. No ports need to be opened on the home network
    3. Cloudflare Tunnel setup is deferred to Phase 2 when Pi hardware integration begins
  </verify>
  <done>
    - SSE events arrive in browser within 1 second of server write (not batched by nginx)
    - MQTT client connects to HiveMQ Cloud and publishes online status
    - All connections are outbound-only -- no ports opened on home network
    - User types "approved" or describes issues for fallback assessment
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` passes all tests (shared + server SSE)
2. `npx tsc --build` compiles all packages with zero errors
3. SSE endpoint responds with correct headers on GET /events
4. Hub MQTT client compiles and imports shared types correctly
5. Manual verification: SSE works on Hostinger (phase gate)
6. Manual verification: MQTT connects to HiveMQ Cloud
</verification>

<success_criteria>
- SSE endpoint tested and deployed, confirmed working on Hostinger (or fallback identified)
- MQTT client configured with LWT, reconnect, and shared topic constants
- Drizzle schema ready for sensor data persistence
- All automated tests pass
- Phase 1 gate cleared: SSE validated on Hostinger
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
