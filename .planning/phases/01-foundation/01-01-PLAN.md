---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - .gitignore
  - .env.example
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/shared/src/topics.ts
  - packages/shared/src/payloads.ts
  - packages/shared/src/topics.test.ts
  - packages/shared/src/payloads.test.ts
  - packages/hub/package.json
  - packages/hub/tsconfig.json
  - packages/hub/src/index.ts
  - packages/server/package.json
  - packages/server/tsconfig.json
  - packages/server/src/index.ts
  - packages/web/package.json
  - packages/web/tsconfig.json
  - packages/web/src/main.tsx
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "tsc --build from root compiles all four packages without errors"
    - "packages/hub and packages/server can import { TOPICS, RETAIN } from @johnnyredis/shared"
    - "Zod payload schemas validate well-formed messages and reject malformed ones"
    - "npx vitest run passes all unit tests for shared package"
  artifacts:
    - path: "package.json"
      provides: "npm workspaces root with packages/* glob"
      contains: "workspaces"
    - path: "tsconfig.json"
      provides: "Root TypeScript project references"
      contains: "references"
    - path: "packages/shared/src/topics.ts"
      provides: "MQTT topic builder functions and retain policy"
      exports: ["TOPICS", "RETAIN"]
    - path: "packages/shared/src/payloads.ts"
      provides: "Zod schemas for MQTT message payloads"
      exports: ["SensorPayload", "CommandPayload", "StatusPayload"]
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
      contains: "projects"
  key_links:
    - from: "packages/hub/package.json"
      to: "@johnnyredis/shared"
      via: "npm workspace dependency"
      pattern: "@johnnyredis/shared"
    - from: "packages/server/package.json"
      to: "@johnnyredis/shared"
      via: "npm workspace dependency"
      pattern: "@johnnyredis/shared"
---

<objective>
Scaffold the npm workspaces monorepo with four packages (shared, hub, server, web), configure TypeScript project references for incremental builds, create the shared MQTT topic constants and Zod payload schemas, and set up vitest for unit testing.

Purpose: Establish the build foundation so all subsequent plans can import shared types and build cleanly. The shared package defines the contract between hub and server -- it must exist first.
Output: A building monorepo with tested shared types, ready for external service integration.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold monorepo and shared package with MQTT contracts</name>
  <files>
    package.json, tsconfig.json, .gitignore, .env.example, vitest.config.ts,
    packages/shared/package.json, packages/shared/tsconfig.json,
    packages/shared/src/index.ts, packages/shared/src/topics.ts,
    packages/shared/src/payloads.ts,
    packages/shared/src/topics.test.ts, packages/shared/src/payloads.test.ts
  </files>
  <behavior>
    - TOPICS.sensor('motion', 'uno') returns 'home/sensor/motion/uno'
    - TOPICS.command('led', 'uno') returns 'home/cmd/led/uno'
    - TOPICS.status('hub') returns 'home/status/hub'
    - TOPICS.heartbeat() returns 'home/hub/heartbeat'
    - RETAIN.sensor is false, RETAIN.status is true
    - SensorPayload.parse({ device: 'motion', board: 'uno', value: 1, ts: 123 }) succeeds
    - SensorPayload.parse({ device: 'motion' }) throws ZodError (missing fields)
    - StatusPayload.parse({ online: true, ts: 123 }) succeeds
    - CommandPayload validates command payloads with device, board, action, and value fields
  </behavior>
  <action>
    1. Create root package.json: name "johnnyredis", private true, workspaces ["packages/*"], scripts: build ("tsc --build"), test ("vitest run").
    2. Create root tsconfig.json with composite project references pointing to all four packages. Use "files": [] at root since it only orchestrates references.
    3. Create .gitignore: node_modules, dist, .env, *.tsbuildinfo.
    4. Create .env.example listing all env vars needed across packages: HIVEMQ_HOST, HIVEMQ_USER, HIVEMQ_PASS, DATABASE_URL, DATABASE_POOLER_URL.
    5. Create vitest.config.ts at root using the Vitest 3.x `projects` array (NOT deprecated vitest.workspace.ts). Point projects to packages/shared (and later packages/server).
    6. Create packages/shared/package.json: name "@johnnyredis/shared", main "src/index.ts", types "src/index.ts". Dependencies: zod.
    7. Create packages/shared/tsconfig.json: composite true, declaration true, declarationMap true, outDir "./dist", rootDir "./src", moduleResolution "node16", module "node16", target "ES2022".
    8. Create packages/shared/src/topics.ts: export TOPICS object with sensor, command, status, heartbeat functions. Export RETAIN object with per-topic retain policy (sensor: false, command: false, status: true, heartbeat: false). Follow exact pattern from RESEARCH.md Pattern 2.
    9. Create packages/shared/src/payloads.ts: Zod schemas for SensorPayload (device string, board string, value number, ts number), StatusPayload (online boolean, ts number), CommandPayload (device string, board string, action string, value union of string/number/boolean), HeartbeatPayload (ts number). Export both schemas and inferred TypeScript types.
    10. Create packages/shared/src/index.ts: re-export everything from topics.ts and payloads.ts.
    11. Write tests FIRST (TDD red phase): packages/shared/src/topics.test.ts testing all topic builders return correct strings and retain values. packages/shared/src/payloads.test.ts testing valid payloads pass and malformed payloads throw.
    12. Run tests (must fail initially since no implementation yet -- but since scaffold creates both, ensure tests run and pass after implementation).
    13. Install dependencies: run `npm install` from root, then `npm install -D vitest` at root, `cd packages/shared && npm install zod`.

    IMPORTANT: Use moduleResolution "node16" (not "node") for Node.js packages per RESEARCH.md State of the Art. Use moduleResolution "bundler" for the web package.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/JohnnyRedis && npm install && npx vitest run packages/shared</automated>
  </verify>
  <done>
    - Root package.json has workspaces config
    - vitest.config.ts exists with projects array
    - Shared package exports TOPICS, RETAIN, and Zod payload schemas
    - All topic builder tests pass (correct strings, correct retain values)
    - All payload schema tests pass (valid data accepted, malformed rejected)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create hub, server, and web package scaffolds with cross-package imports</name>
  <files>
    packages/hub/package.json, packages/hub/tsconfig.json, packages/hub/src/index.ts,
    packages/server/package.json, packages/server/tsconfig.json, packages/server/src/index.ts,
    packages/web/package.json, packages/web/tsconfig.json, packages/web/src/main.tsx
  </files>
  <action>
    1. Create packages/hub/package.json: name "@johnnyredis/hub", private true, dependencies: "@johnnyredis/shared": "*", mqtt, dotenv, zod, tsx. devDependencies: typescript, @types/node. Script: "start": "tsx src/index.ts". Do NOT install serialport yet (requires Pi hardware for native bindings -- will be added in Phase 2 on the Pi).
    2. Create packages/hub/tsconfig.json: composite true, declaration true, declarationMap true, outDir "./dist", rootDir "./src", moduleResolution "node16", module "node16", target "ES2022", references: [{ "path": "../shared" }].
    3. Create packages/hub/src/index.ts: import { TOPICS, RETAIN } from '@johnnyredis/shared' and log them to confirm cross-package import works. Placeholder entrypoint.
    4. Create packages/server/package.json: name "@johnnyredis/server", private true, dependencies: "@johnnyredis/shared": "*", express, mqtt, @neondatabase/serverless, drizzle-orm, dotenv, zod. devDependencies: typescript, @types/node, @types/express, drizzle-kit. Script: "start": "tsx src/index.ts".
    5. Create packages/server/tsconfig.json: same as hub but references: [{ "path": "../shared" }].
    6. Create packages/server/src/index.ts: import { TOPICS } from '@johnnyredis/shared', create a minimal Express app that listens on PORT (env or 3000), with a GET /health endpoint returning { status: 'ok' }. Placeholder entrypoint.
    7. For web package: run `npm create vite@latest packages/web -- --template react-ts` if the directory is empty, OR manually create package.json, tsconfig.json, and src/main.tsx with a minimal React app. The web package tsconfig should use moduleResolution "bundler" (not node16) per Vite requirements.
    8. Update root tsconfig.json references to include all four packages if not already.
    9. Run `npm install` from root to link all workspaces.
    10. Run `tsc --build` from root and verify zero errors.

    NOTE: serialport is intentionally excluded from hub -- it requires native compilation on the target Pi hardware. It will be added in Phase 2 when hub development begins on the Pi.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/JohnnyRedis && npm install && npx tsc --build && npx vitest run</automated>
  </verify>
  <done>
    - tsc --build compiles all four packages with zero errors
    - hub/src/index.ts successfully imports from @johnnyredis/shared (no type errors)
    - server/src/index.ts successfully imports from @johnnyredis/shared (no type errors)
    - All existing vitest tests still pass
    - npm workspaces resolves all cross-package dependencies
  </done>
</task>

</tasks>

<verification>
1. `tsc --build` from root exits with code 0
2. `npx vitest run` passes all shared package tests
3. Cross-package imports resolve correctly (hub and server import from @johnnyredis/shared without errors)
4. `npm ls @johnnyredis/shared` shows hub and server both depend on shared
</verification>

<success_criteria>
- Monorepo with 4 packages builds cleanly via tsc --build
- Shared MQTT topics and Zod payload schemas are exported and tested
- Hub and server packages can import shared types
- Vitest configured and running
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
